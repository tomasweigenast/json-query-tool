name: json_query_tool
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.18.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  code_text_field: ^1.0.2
  google_fonts: ^3.0.1
  flutter_hooks: ^0.18.5+1
  stacked: ^3.0.0
  file_picker: ^5.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: